# Data Collection

Scaffolded stub for the data collection document.

Document the raw data source, its schema, the local raw cache layout, the
idempotent download behavior, and the CLI command to trigger collection.
