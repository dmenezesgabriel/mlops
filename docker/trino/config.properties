# Single-node Trino coordinator config (locked from trinodb/trino:483 defaults;
# catalog.management resolved from the image default ENV=static).
coordinator=true
node-scheduler.include-coordinator=true
discovery.uri=http://localhost:8080
catalog.management=static