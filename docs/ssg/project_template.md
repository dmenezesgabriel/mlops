# Project Template

Each ML project owns configs, data directories, notebooks, documentation, pipeline code, and tests.

Required pipeline steps:

- collect
- preprocess
- features
- train
- tune
- evaluate
- deploy
- monitor

The root `site/site.yaml` can render project notebooks and documentation into an educational site.
